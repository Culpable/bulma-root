# Implementation Plans

Generated by the improve skill (`/improve next` — direction-focused audit) on 2026-06-12 at commit `e308003`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

These are advisor handoff plans, distinct from the Cursor plan-mode documents in `.cursor/plans/`.

**Selection note**: this run executed non-interactively, so plans were written for the top 4 direction findings by leverage (the skill's default of "top 3–5"). The fifth finding (Bulma-vs-ChatGPT comparison page) is recorded below without a plan; ask for one if wanted.

## Repo verification baseline (at `e308003`)

- Typecheck: `cd demo && npx tsc --noEmit` → exit 0 (clean).
- Lint: `cd demo && npm run lint` → **12 pre-existing problems (11 errors, 1 warning)**. No plan may use "lint exits clean" as a gate; the gate everywhere is "no NEW problems".
- Build: `cd demo && npm run build` → static export to `demo/out/` AND regenerates the **tracked** `demo/public/sitemap.xml` with fresh `lastmod` timestamps. Commit sitemap.xml only when a plan changes the route set (002, 003, 004); otherwise revert the churn.
- No test suite exists; do not introduce one inside these plans.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Render the Remotion demo video and embed it on the homepage | P1 | M | — | TODO |
| 002 | Add Terms of Service and Security pages, activate footer links | P1 | S | — | TODO |
| 003 | Resolve the orphaned /about page (default: retire) | P2 | S | — | TODO |
| 004 | Spike: per-lender landing pages from the SUPPORTED_LENDERS ledger | P2 | M (spike) | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- No hard dependencies. Soft ordering: 001 and 004 both add content near the homepage section flow — execute 001 first and rebase 004's branch if both run, to avoid conflicts in `demo/src/app/page.tsx` (004 only reads it, so conflicts are unlikely but cheap to avoid).
- 002 and 003 both edit `demo/src/lib/sitemap.js` and regenerate `demo/public/sitemap.xml`; if run concurrently on separate branches, the second to merge must rebuild the sitemap.
- Human gates: 001 (video content/placement approval), 002 (legal copy review), 003 (page deletion sign-off), 004 (owner answers to the spike's open questions before any rollout). All four plans land on `advisor/*` branches and are never pushed/merged by the executor.

## Direction findings behind these plans (evidence summary)

1. **[DIR-01 → Plan 001]** Complete Remotion demo video (`video/bulma-demo/`, 5 scenes, 30s, documented in `documents/guides/_demo-video.md`) never rendered or embedded; unused `trackVideoPlay` helper at `demo/src/lib/analytics.js:57`; "See it in action" and "Book a demo" CTAs both dead-end at the plain `/contact` form. Confidence HIGH.
2. **[DIR-03 → Plan 002]** Commented-out "Terms of Service" and "Security" footer links at `demo/src/app/layout.tsx:160-161` on a site selling $49–$99/mo subscriptions; homepage FAQ asserts AES-256/TLS 1.3 with no page substantiating it (`demo/src/app/page.tsx:153`). Confidence HIGH.
3. **[DIR-04 → Plan 003]** `/about` is unlinked (nav links removed in `ef4385f`) but still in the sitemap, and its team grid is verbatim Tailwind template placeholders ("Leslie Alexander", "Tom Cook", … at `demo/src/app/about/page.tsx:92-193`). Confidence HIGH.
4. **[DIR-02 → Plan 004]** Canonical 36-lender ledger with URL-ready slugs (`demo/src/lib/supported-lenders.ts`) used only as DOM keys, while lender coverage is the site's main hook (hero badge, lender field, FAQ list, Solo plan copy) — per-lender SEO pages are one `[slug]` route away. Confidence MED-HIGH (mechanics certain; content ROI needs the spike). Effort estimates are coarse, as direction findings are.
5. **[DIR-05 — no plan]** "Bulma vs ChatGPT" comparison page: the objection is already the #2 homepage FAQ (`demo/src/app/page.tsx:117-120`) and differentiation copy (grounding, source attribution) exists site-wide; a dedicated page would target high-intent "vs ChatGPT" queries. Effort M (copy-heavy). Confidence MED. Not planned this run — below the top-4 leverage cutoff.

## Findings considered and rejected

- **Revive email capture / newsletter**: `email-signup-form.tsx` is unused, but `NewsletterForm` is explicitly `@deprecated` (`demo/src/components/sections/footer-with-newsletter-form-categories-and-social-icons.tsx:75`) and the footer form was deliberately removed in `41b66ad` — retired by choice, not unfinished. (Deleting the dead element is a tech-debt nit, out of scope for a direction run.)
- **Market the "Policy Exceptions" capability**: `documents/_app_description.md:74` marks the exception layer "Coming Soon"/under active development — marketing an unshipped feature is premature; revisit when the product ships it.
- **Root `components/` + `pages/` template trees diverging from `demo/src/components`**: real drift, but tech debt rather than direction; this `next`-variant run audited only the direction category. A standard-effort run should pick it up.

## Not audited in this run

Per the `next` invocation variant, only the direction category was audited (plus recon). Correctness, security, performance, tests, tech debt, dependencies, DX, and docs were NOT systematically audited — the 11 pre-existing lint errors noted in the baseline suggest a standard run would find material items.
