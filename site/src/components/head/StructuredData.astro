---
import { serialiseJsonLd } from '../../lib/json-ld.ts';

interface Props {
  graph: unknown;
}

const { graph } = Astro.props;
const json = serialiseJsonLd(graph);
---

<script is:inline type="application/ld+json" set:html={json}></script>
