---
/**
 * Ordered registry for provider-owned snippets that must appear immediately after <body>.
 * Import provider wrapper components above and render them below in exact source order.
 */
---

<!-- Render imported sitewide body-start components here in required order. -->
